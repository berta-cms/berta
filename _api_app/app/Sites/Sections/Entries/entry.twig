<{{ entryHTMLTag }}
  id="{{ entryId }}"
  {% if isEditMode and templateName == 'messy' and not isResponsive %}data-path="{{apiPath}}content/positionXY"{% endif %}
  class="{{ classList }}"
  {% if styleList %}style="{{ styleList }}"{% endif %}>

  {{ entryContents|raw }}
</{{ entryHTMLTag }}>
